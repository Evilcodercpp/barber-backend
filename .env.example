# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=barber

# Или единая строка подключения (приоритет выше)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/barber?sslmode=disable

# Сервер
PORT=8080

# Фронтенд (для CORS)
FRONTEND_URL=http://localhost:3000
